description: "Mainnet from genesis with the client's own link at 8 Mbit and 500ms of added egress delay (~+500ms RTT). One cell of the bandwidth x latency matrix."
mode: mainnet
max_peers: 3

client:
  latency_ms: 500
  rate_kbit: 8000
