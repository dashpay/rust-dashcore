description: "Mainnet from genesis with the client's own link capped at 50 Mbit each way and 1000ms of one-way delay (~+1000ms RTT). One cell of the bandwidth x latency matrix."
mode: mainnet
max_peers: 3

client:
  latency_ms: 1000
  rate_kbit: 50000
